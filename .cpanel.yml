---
deployment:
  tasks:
    - export DEPLOYPATH=/home/smartmet/SmartMetersWebsiteCode/
    - /bin/cp -Rf dist $DEPLOYPATH
    - /bin/cp -f server.js $DEPLOYPATH
    - /bin/cp -f package.json $DEPLOYPATH
    - /bin/cp -f package-lock.json $DEPLOYPATH
    - cd $DEPLOYPATH && npm ci --omit=dev